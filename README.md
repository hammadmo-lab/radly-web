# Radly Frontend

Next.js 15 + TypeScript app (to be scaffolded in Cursor).
This repo intentionally includes only public env examples.

## Env
- Copy `.env.example` to `.env.local` for local dev.
- Do **not** commit `.env.local` or any real envs.

## Dev
Scaffold instructions will follow after design discussion.
